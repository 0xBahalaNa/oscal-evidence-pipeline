# ⚠ Does NOT install standalone: `pip install -r requirements.txt` raises
# ResolutionImpossible (compliance-trestle wants pydantic>=2; we pin <2 for
# oscal-pydantic's frozen v1 Field(regex=...)). For a working install use the
# complete lock with --no-deps:  pip install --no-deps -r requirements.lock
# This file documents DIRECT-dependency intent; requirements.lock is the
# reproducible CM-3 install artifact. Bump intentionally; never loosen these
# pins without a PR review.
#
# compliance-trestle 4.0.3 declares pydantic[email]>=2 in metadata but imports
# via pydantic.v1 at runtime; we pin pydantic<2 for oscal-pydantic.

compliance-trestle==4.0.3
# Published NIST OSCAL JSON Schema gate dependency (tests/test_schema_validation.py).
jsonschema>=4.0
oscal-pydantic==2023.3.21
# oscal-pydantic 2023.3.21 requires pydantic v1 Field(regex=...); v2 breaks import.
pydantic>=1.10.13,<2
pytest==8.4.2
pytest-cov==6.3.0
mypy==1.20.2
